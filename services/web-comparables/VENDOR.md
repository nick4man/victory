# Владение

`SKILL.md` — скилл оценки недвижимости по аналогам (`real-estate-estimator`,
v4.0 Live Search): классификация объекта, источники по типам, поиск живых
аналогов, структурированный отчёт со ссылками.

Пришёл из openclaw (`skills/real-estate-estimator/`). С 11.09.26 openclaw —
архив, вендоринг прекращён, **victory владелец**. На момент заморозки копии
были байт-в-байт идентичны — забирать нечего, сверять не с чем.

В архиве рядом со скиллом лежал `scripts/` — там только устаревший
`__pycache__/parse_real.cpython-313.pyc` без исходника. Не переносили.

Соседи по теме оценки и аудита — `services/audit-engine/` (движок
инвест-аудита, свой `VENDOR.md`) и Ruby-часть `app/services/audit_pdf/`.
