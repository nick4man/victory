Вход в личный кабинет — <%= AgencyInfo::NAME %>

Здравствуйте<%= @user.first_name.present? ? ", #{@user.first_name}" : '' %>.

Чтобы войти в личный кабинет, перейдите по ссылке:
<%= @url %>

Ссылка действительна <%= @expires_in_min %> минут и работает только один раз.

Если вы не запрашивали этот вход — просто проигнорируйте это письмо.

---
<%= AgencyInfo::NAME %>
<%= AgencyInfo::WEBSITE_URL %>
