<% content_for :title, "Редактирование отчёта" %>
<%= render 'shared/dashboard_nav' %>
<section class="px-6 lg:px-12 pt-12 pb-20 bg-background min-h-screen">
  <div class="max-w-5xl mx-auto">
    <p class="text-sm tracking-[0.4em] text-muted-foreground mb-3">АДМИНИСТРИРОВАНИЕ · ОТЧЁТ #<%= @report.id %></p>
    <h1 class="text-3xl lg:text-4xl font-light tracking-wide text-foreground mb-12 pb-8 border-b border-border"><%= @report.title %></h1>
    <%= render 'form' %>
  </div>
</section>
