# EOL Upgrade App Code (Phase 1b) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Bump Ruby 3.2.2 → 3.3.6 + Rails 7.1.6 → 7.2.3.1 + 3 dependent gems в victory62 app code. Закрывает 8 Rails CVE + 2 Brakeman EOL HIGH warnings.

**Architecture:** Single branch с 5 per-step commits для clean rollback. Each step verified локально (Docker + bin/rspec) перед next. Final smoke testing на staging.victory62.org (требует Plan 2 merged).

**Tech Stack:** Ruby 3.3.6 (YJIT default), Rails 7.2.3.1 (load_defaults 7.2), ransack 4.2, neighbor 0.6 (pgvector), bcrypt 3.1.22.

**Pre-requisites:**
- Plan 1 (test-bootstrap) merged — ~100 specs green baseline
- Plan 2 (staging-setup) merged — staging.victory62.org доступен для validation

---

## File Structure

**Modified files (5):**
- `Dockerfile` — Ruby image
- `Gemfile` — Ruby version + rails + ransack + neighbor + bcrypt
- `Gemfile.lock` — auto-regenerated by bundle
- `config/application.rb` — load_defaults flip
- `config/initializers/new_framework_defaults_7_2.rb` (NEW, auto-created by `app:update`)

**New files (1):**
- `.ruby-version` — pin для rbenv/asdf

**Branch:** `feat/eol-phase-1-ruby33-rails72`

---

## Pre-flight

- [ ] **Step 0a: Verify pre-requisites**

Both должны быть merged в main:
- Plan 1 (test-bootstrap) PR — merged
- Plan 2 (staging-setup) PR — merged + staging.victory62.org живой

```bash
gh pr list --state merged --search "test bootstrap" --limit 1
gh pr list --state merged --search "setup staging" --limit 1
curl -u 'victory:<pass>' -sI https://staging.victory62.org/  # HTTP/2 200
```

- [ ] **Step 0b: Create branch**

```bash
git checkout main && git pull
git checkout -b feat/eol-phase-1-ruby33-rails72
```

- [ ] **Step 0c: Verify baseline green**

```bash
docker compose run --rm web bin/rspec
docker compose run --rm web bin/rubocop
```

Expected: ~100 specs pass, RuboCop с baseline pass. Если нет — fix перед start.

---

## Task 1: Audit greps + .ruby-version

**Files:**
- Create: `.ruby-version`

- [ ] **Step 1: Audit для surprises**

```bash
grep -rn 'Rails.application.secrets' app config lib  # expect 0
grep -rn 'Arel.sql' app/services app/models           # review each
grep -rn 'load_defaults' config/                       # expect 1 (application.rb)
```

If `Rails.application.secrets` found — fix перед upgrade (use `Rails.application.credentials` instead).
If `Arel.sql` with interpolation found — review (Rails 7.2 strict-er parser).

- [ ] **Step 2: Create .ruby-version**

```bash
echo '3.3.6' > .ruby-version
```

- [ ] **Step 3: Commit**

```bash
git add .ruby-version
git commit -m "chore(ruby): add .ruby-version=3.3.6"
```

---

## Task 2: Bump Ruby в Dockerfile + Gemfile

**Files:**
- Modify: `Dockerfile`
- Modify: `Gemfile`

- [ ] **Step 1: Read Dockerfile**

Run: `head -5 Dockerfile`
Expected current: `FROM ruby:3.2.2-slim-bookworm`

- [ ] **Step 2: Edit Dockerfile**

Change first line:

```diff
- FROM ruby:3.2.2-slim-bookworm
+ FROM ruby:3.3.6-slim-bookworm
```

- [ ] **Step 3: Edit Gemfile**

Change line 6:

```diff
- ruby '3.2.2'
+ ruby '3.3.6'
```

- [ ] **Step 4: Backup Gemfile.lock + rebuild**

```bash
cp Gemfile.lock /tmp/Gemfile.lock.pre-ruby33.bak
docker compose build web         # 3-5 min — pulls Ruby 3.3.6 image
```

Expected: build succeeds, bundle install inside container resolves OK.

- [ ] **Step 5: Run RSpec**

```bash
docker compose run --rm web bin/rspec
```

Expected: ~100 specs still pass. Если failures — diagnose Ruby 3.3 compat:
- Common: kwargs warnings (usually OK)
- Common: encoding deprecation warnings (review)
- Common: gem requires Ruby version constraint failure (note для Task 4)

- [ ] **Step 6: Commit**

```bash
git add Dockerfile Gemfile Gemfile.lock
git commit -m "chore(ruby): bump Ruby 3.2.2 → 3.3.6"
```

---

## Task 3: Bump Rails 7.1.6 → 7.2.3.1

**Files:**
- Modify: `Gemfile`
- Modify: `Gemfile.lock` (auto)
- Modify: `config/application.rb`

- [ ] **Step 1: Edit Gemfile rails line**

```diff
- gem 'rails', '~> 7.1.0'
+ gem 'rails', '~> 7.2.3', '>= 7.2.3.1'
```

- [ ] **Step 2: Backup + bundle update rails**

```bash
cp Gemfile.lock /tmp/Gemfile.lock.pre-rails72.bak
docker compose run --rm web bundle update rails
```

Expected: resolves to `rails 7.2.3.1`, plus matching actionview/activerecord/etc.

Если resolution fails — note дополнительные gems которые pin'аются. Common conflict: `ransack ~> 4.1` won't allow Rails 7.2 (fixed в Task 4) — если так, отложить rails update до Task 4, или сделать combined update.

- [ ] **Step 3: Run rails app:update interactively**

```bash
docker compose run --rm web bin/rails app:update
```

При интерактивных prompts:
- `bin/rails` — `Y` (regen scripts)
- `bin/setup` / `bin/dev` — `Y`
- `config/environments/*.rb` — `n` (review каждый отдельно через `bin/rails app:update:configs`)
- `config/initializers/new_framework_defaults_7_2.rb` — `Y` (creates file, мы review дальше)
- `config/storage.yml` — `n` (наш cloud config)
- `config/cable.yml` — `n` (наш redis url)
- `config/application.rb` — `n` (manual edit ниже)

- [ ] **Step 4: Manually flip load_defaults 7.2**

Edit `config/application.rb`:

```diff
- config.load_defaults 7.1
+ config.load_defaults 7.2
```

- [ ] **Step 5: Review new_framework_defaults_7_2.rb**

Run: `cat config/initializers/new_framework_defaults_7_2.rb`

Каждая опция (uncomment to enable in 7.2 defaults):
- `Rails.application.config.active_storage.web_image_content_types` — review carefully (наш `image_processing` config может зависеть)
- `Rails.application.config.action_dispatch.debug_exception_log_level` — accept default
- Любые другие options — accept defaults для maximum 7.2 behavior, oставить `false` если опция конфликтует с нашим code.

Если есть сомнения — leave file as-is (всё закомментировано = вся 7.2 поведение из load_defaults).

- [ ] **Step 6: Run RSpec**

```bash
docker compose run --rm web bin/rspec
```

Expected: ~100 specs pass. Common failures Rails 7.2:
- `redirect_back` deprecated → `redirect_back_or_to` (we have ~15, in rubocop_todo) — should be warnings only
- `Rails.application.secrets` removed — confirmed 0 via audit
- ActiveStorage proxy changes — verify if `_picture` partial affected

- [ ] **Step 7: Smoke boot**

```bash
docker compose up -d web
docker compose logs web --tail 50
```

Expected: Puma boots cleanly, no panic.

```bash
curl -sI http://localhost:3000/
```

Expected: HTTP/1.1 200 (или 302).

- [ ] **Step 8: Commit**

```bash
git add Gemfile Gemfile.lock config/application.rb config/initializers/new_framework_defaults_7_2.rb bin/
git commit -m "chore(rails): bump Rails 7.1.6 → 7.2.3.1 + flip load_defaults 7.2"
```

---

## Task 4: Bump ransack 4.1 → 4.2

**Files:**
- Modify: `Gemfile`
- Modify: `Gemfile.lock` (auto)

- [ ] **Step 1: Edit Gemfile ransack line**

```diff
- gem 'ransack', '~> 4.1'
+ gem 'ransack', '~> 4.2'
```

- [ ] **Step 2: Bundle update**

```bash
docker compose run --rm web bundle update ransack
```

Expected: resolves to `ransack 4.2.x`.

- [ ] **Step 3: Smoke ransack usage**

Find any controller using `ransack`:

```bash
grep -rn 'ransack\|search:' app/controllers | head -5
```

If found admin/search etc — smoke via curl/admin panel locally.

- [ ] **Step 4: Run RSpec**

```bash
docker compose run --rm web bin/rspec
```

- [ ] **Step 5: Commit**

```bash
git add Gemfile Gemfile.lock
git commit -m "chore(deps): bump ransack 4.1 → 4.2 for Rails 7.2 compat"
```

---

## Task 5: Bump neighbor 0.5 → 0.6

**Files:**
- Modify: `Gemfile`
- Modify: `Gemfile.lock` (auto)

- [ ] **Step 1: Edit Gemfile**

```diff
- gem 'neighbor', '~> 0.5'
+ gem 'neighbor', '~> 0.6'
```

- [ ] **Step 2: Bundle update**

```bash
docker compose run --rm web bundle update neighbor
```

- [ ] **Step 3: Smoke nearest_neighbors usage**

```bash
docker compose run --rm web bin/rails runner \
  "puts PropertyEmbedding.nearest_neighbors(:embedding, [0.0]*768, distance: :cosine).limit(1).to_sql"
```

Expected: valid SQL output. Если API changed — check neighbor 0.6 CHANGELOG.

- [ ] **Step 4: Run RSpec** (especially semantic search specs from Plan 1)

```bash
docker compose run --rm web bin/rspec spec/services/chat_tools/staff/
```

Expected: search_group_messages + search_all_leads specs green.

- [ ] **Step 5: Commit**

```bash
git add Gemfile Gemfile.lock
git commit -m "chore(deps): bump neighbor 0.5 → 0.6 for pgvector compat"
```

---

## Task 6: Bump bcrypt 3.1.7 → 3.1.22

**Files:**
- Modify: `Gemfile`
- Modify: `Gemfile.lock` (auto)

- [ ] **Step 1: Edit Gemfile**

```diff
- gem 'bcrypt', '~> 3.1.7'
+ gem 'bcrypt', '~> 3.1', '>= 3.1.22'
```

- [ ] **Step 2: Bundle update**

```bash
docker compose run --rm web bundle update bcrypt
```

- [ ] **Step 3: Run RSpec**

```bash
docker compose run --rm web bin/rspec
```

Expected: ~100 specs pass.

- [ ] **Step 4: Commit**

```bash
git add Gemfile Gemfile.lock
git commit -m "chore(deps): bump bcrypt 3.1.7 → 3.1.22 (CVE clean)"
```

---

## Task 7: Local Brakeman + bundler-audit verify

**Files:**
- (none modified)

- [ ] **Step 1: Run Brakeman**

```bash
docker compose run --rm web bin/brakeman --exit-on-warn --quiet --format text
```

Expected outcome:
- EOL Rails warning — GONE
- EOL Ruby warning — GONE
- 2 SQL injection HIGH (search_*) — GONE (already fixed)
- 1 XSS HIGH (case_studies) — GONE (already fixed)
- Remaining: UnsafeReflection Medium + 4 weak warnings = 5 total

Brakeman exit code: still 3 (exit-on-warn), но total count significantly reduced.

- [ ] **Step 2: Run bundler-audit**

```bash
docker compose run --rm web bin/bundle-audit update
docker compose run --rm web bin/bundle-audit check
```

Expected outcome:
- 0 Rails-related CVE (all bumped)
- 0 rack/rack-session CVE (already bumped)
- 0 bcrypt CVE (bumped)
- May have warnings about other gems' patches — non-blocking unless flagged как critical.

- [ ] **Step 3: Run final RSpec**

```bash
docker compose run --rm web bin/rspec
```

Expected: 100 examples, 0 failures.

- [ ] **Step 4: Document local verification results**

(No commit — just take notes:
- Brakeman warnings count: __
- bundler-audit warnings count: __
- RSpec result: __ examples, __ failures)

---

## Task 8: Push branch + open PR

**Files:**
- (none — git op)

- [ ] **Step 1: Push branch**

```bash
git push -u origin feat/eol-phase-1-ruby33-rails72
```

- [ ] **Step 2: Open PR**

```bash
gh pr create --base main --title "feat(eol): Phase 1 — Ruby 3.3.6 + Rails 7.2.3.1" \
  --body "Phase 1b from docs/superpowers/specs/2026-06-04-eol-rails-ruby-upgrade-design.md

Closes:
- 8 Rails-related CVE (Action View XSS, Active Storage path traversal, etc — all need 7.2.3.1+)
- 2 Brakeman EOL HIGH warnings (Rails 7.1.6 EOL'd 2025-10-01, Ruby 3.2.2 EOL'd 2026-03-31)

Bumps:
- Ruby 3.2.2 → 3.3.6 (YJIT default on, perf bonus)
- Rails 7.1.6 → 7.2.3.1 + load_defaults 7.2 flip
- ransack 4.1 → 4.2 (Rails 7.2 compat)
- neighbor 0.5 → 0.6 (pgvector compat)
- bcrypt 3.1.7 → 3.1.22 (CVE clean)

Pre-requisites: Plans 1 (test-bootstrap) + 2 (staging) merged.
Validation: см. design doc Section 6 Layer 2 (staging.victory62.org).

Phase 2 (Rails 8 + Ruby 3.4) — отдельный спринт."
```

- [ ] **Step 3: Verify CI**

Wait для CI run. Expected:
- RuboCop ✓ (baseline)
- Brakeman ✓ (значительно меньше warnings, exit 3 если EOL warnings были единственным blocker — verify)
- bundler-audit ✓ (Rails CVE closed)
- GitGuardian ✓

If still red:
- Brakeman: review remaining warnings — UnsafeReflection + 4 weak ok для merge (out of scope)
- bundler-audit: verify rack/rack-session/sidekiq-cron уже bumped в prior PRs

- [ ] **Step 4: Don't merge yet** — Wait for staging validation (next Task)

---

## Task 9: Deploy to staging.victory62.org

**Files:**
- (server-side ops)

- [ ] **Step 1: SSH к VDS + pull branch**

```bash
ssh vds 'cd /path/to/victory && git fetch origin feat/eol-phase-1-ruby33-rails72 \
  && git checkout feat/eol-phase-1-ruby33-rails72'
```

- [ ] **Step 2: Build staging image**

```bash
ssh vds 'docker compose -f docker-compose.staging.yml build web-staging'
```

Expected: build succeeds (использует new Ruby 3.3.6 image + Rails 7.2.3.1 gems).

- [ ] **Step 3: Run migrations**

```bash
ssh vds 'docker compose -f docker-compose.staging.yml run --rm web-staging bin/rails db:migrate'
```

Expected: no pending migrations (Rails upgrade обычно не добавляет миграции, но verify).

- [ ] **Step 4: Restart staging stack**

```bash
ssh vds 'docker compose -f docker-compose.staging.yml up -d'
ssh vds 'docker compose -f docker-compose.staging.yml logs web-staging --tail 30'
```

Expected: Puma boots clean. No deprecation panic.

- [ ] **Step 5: Smoke staging**

```bash
curl -u 'victory:<pass>' -sI https://staging.victory62.org/
curl -u 'victory:<pass>' -sI https://staging.victory62.org/admin/health.json?token=$STAGING_ADMIN_TOKEN
```

Expected: 200/302 OK responses.

- [ ] **Step 6: Run live-test-playbook на staging**

См. `.claude/docs/live-test-playbook.md`. Минимум:
- Group A — read-only health (admin/health.json checks)
- Property search smoke
- Mortgage calculator smoke
- TG bot — но **на staging** (если есть staging TG group, иначе skip)

- [ ] **Step 7: 3-day acceptance period**

Документировать любые anomalies в Sentry/Sidekiq за 3 рабочих дня. Если 0 issues — go to Task 10. Если issues — fix on branch + повторить step 5-6.

---

## Task 10: Prod deploy + 24h watch

**Files:**
- (server-side ops + observability)

- [ ] **Step 1: Pre-deploy checklist (см. design Section 7)**

10 items, всех должны быть ✅:
1. Sidekiq queue empty: `ssh vds 'docker compose exec -T web bin/rails runner "puts Sidekiq::Queue.all.map { |q| [q.name, q.size] }"'` → все 0
2. Cron sanity: `puts Sidekiq::Cron::Job.all.map(&:name)` → ожидаемые ~15 jobs
3. No pending migrations: `bin/rails db:migrate:status | grep down` → empty
4. DB backup: `pg_dump > /tmp/pre-eol-backup.sql && rclone copy nxt:Офис/Backups/`
5. Rollback tag: `docker tag viktory-web:latest viktory-web:prod-pre-eol-phase-1`
6. Staging acceptance completed (3 рабочих дня clean)
7. Low-traffic window (не 09-12 MSK)
8. Team в TG 2ч после deploy
9. PR CI green
10. Director signoff в TG

- [ ] **Step 2: Announce в staff TG group**

> "EOL Phase 1 deploy через 15 минут — Ruby 3.2→3.3, Rails 7.1→7.2. Watch for anomalies."

- [ ] **Step 3: Merge PR**

```bash
gh pr merge <eol-pr-number> --merge
```

(Squash избегаем для preserve per-step commits.)

- [ ] **Step 4: Prod deploy**

```bash
ssh vds 'cd /path/to/victory && git pull origin main'
ssh vds 'docker compose pull web'
ssh vds 'docker compose up -d web'
ssh vds 'docker compose exec -T web bin/rails db:migrate'  # если pending
```

Wait ~30s для Traefik rolling.

- [ ] **Step 5: Post-deploy smoke (10 minutes)**

```bash
curl -s "https://victory62.org/admin/health.json?token=$ADMIN_TOKEN" | jq .
curl -sI https://victory62.org/
curl -sI https://victory62.org/blog
curl -sI https://victory62.org/kupit/kvartira
```

Expected: все 200 OK + health.json status="ok".

```bash
ssh vds 'docker compose logs web --tail 100 | grep -iE "error|panic|deprecat"'
```

Expected: no panics. Warnings OK.

- [ ] **Step 6: Sidekiq + cron check**

```bash
ssh vds 'docker compose exec -T web bin/rails runner \
  "puts Sidekiq::Cron::Job.all.map(&:name)"'
```

Expected: same ~15 jobs (sidekiq-cron 2.4 compat verified).

- [ ] **Step 7: Announce success в TG**

> "EOL Phase 1 deploy completed @ HH:MM. Watch period 24h — report anomalies."

- [ ] **Step 8: 24-hour watch**

Каждые 2-3 часа:
- `/admin/health.json` checks
- Sentry error rate vs baseline
- Sidekiq queue depth
- Traefik 5xx rate

Если rollback trigger срабатывает (см. design Section 8):
```bash
ssh vds 'docker tag viktory-web:prod-pre-eol-phase-1 viktory-web:latest && docker compose up -d web'
```

---

## Task 11: Update memory + close out

**Files:**
- Modify: `.claude/memory/techContext.md`
- Modify: `.claude/memory/activeContext.md`

- [ ] **Step 1: Update techContext.md**

```bash
# Update Ruby / Rails version lines
sed -i 's/Ruby 3.2.2/Ruby 3.3.6/g; s/Rails 7.1/Rails 7.2.3.1/g' .claude/memory/techContext.md
```

(Verify diff before commit.)

- [ ] **Step 2: Update activeContext.md**

Edit phase / current focus to note "Phase 1 EOL upgrade complete; Phase 2 (Rails 8 + Ruby 3.4) — отдельный sprint".

- [ ] **Step 3: Update CLAUDE.md если есть упоминания версий**

```bash
grep -n 'Ruby 3.2\|Rails 7.1' CLAUDE.md
```

If found — update.

- [ ] **Step 4: Commit memory updates**

```bash
git add .claude/memory/techContext.md .claude/memory/activeContext.md CLAUDE.md 2>/dev/null || true
git commit -m "docs(memory): update techContext after EOL Phase 1 (Ruby 3.3.6, Rails 7.2.3.1)"
git push
```

- [ ] **Step 5: Final acceptance verify**

Все из design Section 10:
- [x] Design approved
- [x] Layer 0 — ~100 specs green
- [x] staging.victory62.org работает
- [x] Branch — 5 steps committed
- [x] Local Layer 1 — все pass
- [x] Staging Layer 2 — 3 рабочих дня без anomalies
- [x] Pre-deploy checklist all ✅
- [x] Production deploy без rollback
- [x] CI green (Rails CVE closed, Brakeman -2 EOL HIGH)
- [x] Memory updated
- [x] Phase 2 backlog item создан

- [ ] **Step 6: Create Phase 2 backlog item**

```bash
gh issue create --title "Phase 2: Rails 7.2 → 8.0 + Ruby 3.3 → 3.4" \
  --body "Continuation of EOL upgrade. See docs/superpowers/specs/2026-06-04-eol-rails-ruby-upgrade-design.md Section 9.

Targets:
- Rails 7.2.3.1 → 8.0.4.1 (или 8.1.x stable)
- Ruby 3.3.6 → 3.4.x
- Devise 4.9 → 5.0 (мажорный, Devise off на проде — consider)
- friendly_id 5.5 → 6.x
- Modern stack: Solid Queue / Solid Cache / Propshaft (vs Sprockets)
- Brakeman: address UnsafeReflection в topnlab_reports + 4 weak (.brakeman.ignore)

Timeline: 2-3 недели TBD."
```

---

## Acceptance

- [x] Spec coverage: Phase 1b + 1c из design Section 3 — covered (5 bumps + load_defaults + audit + brakeman/bundler-audit verify + staging + prod deploy + watch + rollback + memory update + Phase 2 pointer)
- [x] Placeholder scan: `<eol-pr-number>` и `<pass>` помечены как substitution points; remaining content concrete
- [x] Type consistency: gem versions consistent (Ruby 3.3.6, Rails 7.2.3.1, ransack 4.2, neighbor 0.6, bcrypt 3.1.22) across all tasks
