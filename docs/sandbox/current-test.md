# Что сейчас в песочнице

<!-- Этот файл печатает /starttest в боевом боте. Держать коротким: что
     проверяем, что уже починено, на что смотреть. Даты — dd.MM.yy. -->

**Стек карточек CRM (PR #93–#97) + «вставил как есть» (PR #100)**, 19.09.26.

Бот песочницы: **@anvictory_testsbot** — только личка, в рабочую группу не пишет.
Заявки в Topnlab не уходят: вместо номера CRM карточка получает `TEST-<id>`.

Что проверяем:
1. «🧪 Тестовый лид» → вставить текст клиента целиком → «📋 Карточка заявки».
   Имя, телефон и суть просьбы разбираются из текста, мастер спрашивает только
   недостающее.
2. Машинная проверка → «📤 На модерацию» → у модератора «↩️ На доработку» и
   «✅ Одобрить и выгрузить» → `TEST-<id>`.
3. «🏠 Новый объект в CRM» → модерация → «📥 Внесено в CRM» с номером.
4. Телефоны: мусор и нехватка цифр отклоняются на вводе, городской — только
   вторым номером, о дубле бот предупреждает сразу.
5. Изоляция: в рабочем боте тестовых карточек и лидов не видно.
